const { default: axios } = require("axios");
import { Client } from "twitter-api-v2";

const endpoint = "https://graph.x.com";

const  x = {
    getXUserPost: async () => {
        try {
            
            const response = await axios(`${endpoint}/`)

        } catch (error) {
            
        }
    },



}